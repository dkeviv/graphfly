# Local manual testing (recommended "pg mode")
GRAPHFLY_MODE=dev
GRAPHFLY_AUTH_MODE=none
GRAPHFLY_QUEUE_MODE=pg
DATABASE_URL=postgres://graphfly:graphfly@127.0.0.1:5432/graphfly
PG_POOL_MAX=10

# Local manual testing: allow onboarding to create a Project from a local git repo path.
GRAPHFLY_ALLOW_LOCAL_REPO_ROOT=1

# Docs output (local docs git repo)
DOCS_REPO_FULL_NAME=acme/docs
DOCS_REPO_PATH=/absolute/path/to/your/docs-repo

# Optional: make semantic search real locally
GRAPHFLY_EMBEDDINGS_MODE=deterministic
# GRAPHFLY_EMBEDDINGS_MODE=http
# GRAPHFLY_EMBEDDINGS_HTTP_URL=http://127.0.0.1:9999/embed
# GRAPHFLY_EMBEDDINGS_HTTP_TOKEN=

# Optional: LLM (assistant/doc/graph agents)
# OPENROUTER_API_KEY=
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# GRAPHFLY_LLM_MODEL=openai/gpt-4o-mini

# Optional: GitHub Enterprise
# GITHUB_API_BASE_URL=https://api.github.com
