services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: graphfly
      POSTGRES_USER: graphfly
      POSTGRES_PASSWORD: graphfly
    ports:
      - "5432:5432"
    volumes:
      - graphfly_pgdata:/var/lib/postgresql/data

volumes:
  graphfly_pgdata:

